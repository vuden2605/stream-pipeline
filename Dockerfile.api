FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    fastapi \
    "uvicorn[standard]" \
    redis

COPY accident_api.py .
COPY admin_dashboard/ ./admin_dashboard/

CMD ["uvicorn", "accident_api:app", "--host", "0.0.0.0", "--port", "8000"]
